language: python
python: 3.7
dist: xenial
sudo: true
# command to install dependencies
install: "pip install -r requirements.txt"
# command to run tests
script: python manage.py test polls --settings=polls_service.test_settings
